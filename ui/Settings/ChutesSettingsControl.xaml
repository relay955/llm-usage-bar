<UserControl x:Class="LLMUsageBar.ui.ChutesSettingsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="160"
             d:DesignWidth="520">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="260" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0"
                   Grid.Column="0"
                   Text="Chutes 사용"
                   Foreground="#FFE8E8E8"
                   FontSize="13"
                   VerticalAlignment="Center" />

        <CheckBox Grid.Row="0"
                  Grid.Column="1"
                  HorizontalAlignment="Left"
                  VerticalAlignment="Center"
                  IsChecked="{Binding PendingAppSettings.UseChutes, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Grid.Row="1"
                   Grid.Column="0"
                   Margin="0,16,0,0"
                   Text="API Key"
                   Foreground="#FFE8E8E8"
                   FontSize="13"
                   VerticalAlignment="Center" />

        <TextBox Grid.Row="1"
                 Grid.Column="1"
                 Margin="0,16,0,0"
                 Height="30"
                 VerticalContentAlignment="Center"
                 Padding="8,0"
                 Text="{Binding PendingAppSettings.ChutesApiKey, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Grid.Row="2"
                   Grid.Column="0"
                   Margin="0,16,0,0"
                   Text="User ID / Username"
                   Foreground="#FFE8E8E8"
                   FontSize="13"
                   VerticalAlignment="Center" />

        <TextBox Grid.Row="2"
                 Grid.Column="1"
                 Margin="0,16,0,0"
                 Height="30"
                 VerticalContentAlignment="Center"
                 Padding="8,0"
                 Text="{Binding PendingAppSettings.ChutesUserIdOrUsername, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
    </Grid>
</UserControl>
